# quick-catch

quick-catch（一键记事）是一个思源笔记插件，只提供一项功能：通过全局快捷键唤出悬浮记事面板，把内容直接写入默认笔记本。**仅 Windows 支持。**

## 术语

**一键记事 (One-Tap Note)**:
通过全局快捷键 Shift+S 唤出的快速记事。确认后直接在「记事默认笔记本」中生成一篇以时间戳为标题的文档（不创建中间文件夹），创建后不打开。
_Avoid_: 便签, 全局速记, 速记笔记

**记事默认笔记本 (Default Notebook)**:
存放记事文档的笔记本。未设置时按快捷键会打开设置面板。

**记事面板 (One-Tap Panel)**:
独立的置顶小窗口：思源主窗口保持后台不跳转，面板配色跟随思源主题，可拖动并记住位置，Enter 提交、Shift+Enter 换行。

**顶栏按钮 (Top Bar Button)**:
顶栏中的快捷键入口，点击打开设置面板以设定默认笔记本。

**全局快捷键 (Global Hotkey)**:
系统级快捷键，思源在后台时也生效。默认 Shift+S，可在思源「设置 → 快捷键 → 插件」中修改。
